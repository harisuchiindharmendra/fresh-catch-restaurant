'use client';

/**
 * Optional mount point for Spline / Three.js scenes.
 * Kept as a no-op so the hero stays light by default — drop in
 * a <Spline scene="..." /> or a Three.js Canvas here when ready.
 */
export default function Ambient() {
  return null;
}
